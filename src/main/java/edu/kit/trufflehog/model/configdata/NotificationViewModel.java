package edu.kit.trufflehog.model.configdata;

/**
 * <p>
 *     The NotificationViewModel contains the config data for the notification view. That means it contains the labels
 *     and any other information relevant to displaying the notification view.
 * </p>
 * <p>
 *     The notification view is where notification that occurred in TruffleHog are shown. So if for example a node is
 *     suddenly marked as illegal because it was on a blacklist, this would show up in the notification view.
 * </p>
 */
public class NotificationViewModel extends ConfigDataModel {
}
