package edu.kit.trufflehog.model.configdata;

/**
 * <p>
 *     The StatisticsModel contains the config data for the statistic menus. That means it contains the labels
 *     and any other information relevant to displaying the graph and node statistics.
 * </p>
 * <p>
 *     The StatisticsModel is used by both the graph and the node statics view. They both use almost the same settings,
 *     and can thus just pull the data they need form their own sources (node statistics view from the nodes and graph
 *     statistics view from the graph.)
 * </p>
 */
public class StatisticsModel extends ConfigDataModel {
}
